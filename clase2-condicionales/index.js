let mes = 'julio' // enero

// true false
if (nombre === 'tetaz') {
  console.log('paso 1')
} else if (edad >= 18) {
  console.log('paso 2')
} else {
  console.log('paso 3')
}

// Operadores de comparación:

// === : Igualdad estricta (compara valor y tipo).
// == : Igualdad (compara solo el valor).
// !== : Desigualdad estricta (compara valor y tipo).
// != : Desigualdad (compara solo el valor).
// > : Mayor que.
// < : Menor que.
// >= : Mayor o igual que.
// <= : Menor o igual que.

// Operadores lógicos:
// && : AND lógico (ambas condiciones deben ser verdaderas).
// || : OR lógico (al menos una condición debe ser verdadera).
// ! : NOT lógico (niega una condición).

// Operadores aritméticos (a veces usados en condicionales):
// +, -, *, /, % : Para realizar operaciones matemáticas.
